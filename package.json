{
  "name": "cmsqlite",
  "version": "2.0.12",
  "description": "CMSQLite easy SQLite based CMS",
  "main": "index.js",
  "scripts": {
    "copy": "node ./npmtasks/copy.js",
    "admin-dev": "NODE_OPTIONS=--openssl-legacy-provider webpack --config webpack.admin.config.js --watch true",
    "build": "NODE_OPTIONS=--openssl-legacy-provider webpack --config webpack.build.config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.21.1",
    "sweetalert2": "^9.17.2",
    "vue": "^2.6.12"
  },
  "devDependencies": {
    "copy": "^0.3.2",
    "webpack": "^4.46.0",
    "webpack-cli": "^3.3.12"
  }
}
